[isub]
k: #execute => #halt
statusCode: _ => EVMC_SUCCESS
output: _ => #asByteStackInWidth(X -Int Y, 32)
callData: #abiCallData("isub", #int256(X), #int256(Y))
localMem: _
pc: 0 => _
wordStack: .WordStack => _
gas: Gas => _
log: _
callDepth: _
refund: _
storage: _
activeaccounts:
accounts:
requires:
  andBool Gas >Int 100000
  andBool #rangeSInt(256, X)
  andBool #rangeSInt(256, Y)
  // success conditions (in order):
  andBool #rangeSInt(256, X -Int Y)

attribute:

